"""Default configuration constants.

Note: Actual default values are in default.yml which ships with the package.
This module only contains constants used for backward compatibility.
"""

from __future__ import annotations

# Known AI coding agents (used for help text and backward compatibility)
KNOWN_AGENTS = ["claude", "gemini", "codex", "agent", "copilot"]
